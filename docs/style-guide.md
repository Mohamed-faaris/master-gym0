#Style-guide

1. all use animate ui components
   a) if the component is not available, use shadcn/ui components
   b) if neither are available, create a new animate ui component[use shadcn/ui design patterns(colors(style.css variables), spacing, typography, etc)]
2. use Zod for validation
3. lucide icons and tailwindcss for styling
